# Decision Log
<!-- Most valuable file. Most skipped file. Failed experiments go here. -->

| Date | Decision / experiment | Why | Result | Keep or kill |
|---|---|---|---|---|
| 2026-07-31 | Pivot med spas → restoration | Med spas = 0 sales, owners unreachable; restoration values a missed emergency call far more (lost 4–5 fig job) | TBD — outreach not started | Keep (test) |
| 2026-07-31 | Waive $500 setup for first 3 clients for case-study rights; must go live within a week | No proof yet; first clients bought cheaper than any ad to close the proof gap | TBD | Keep (test) |
| 2026-07-31 | Phone-first outreach, not email | Restoration owners live on their phone; live Vapi demo is the hook | TBD — starts 2026-08-01 | Keep (test) |
| 2026-07-31 | NOT building auto-run agent loop or new subagents pre-revenue | Automation multiplies output; output is 0 dials, so it multiplies 0. Selling beats building until 5 clients | Held | Keep |
| 2026-08-07 | Paused new-lead sourcing in the daily morning engine | Pipeline hit 198 restoration leads (2 short of the routine's own 200 hard max), every single one still stage `new`, zero calls logged since 07-31. Fails the CEO decision filter on all 4 counts — sourcing more doesn't increase revenue, removes no bottleneck, adds manual work, doesn't ease scaling. `metrics.md` still shows 1 dial vs. 130 staged as of 07-27, never updated since | TBD — resume sourcing once pipeline.md shows real call outcomes, not just "sourced" | Keep (test) |
| 2026-08-08 | Paused new-lead sourcing again (2nd straight day) | Condition from 08-07 hasn't changed — it got worse. Pipeline sits at 398 leads (198 restoration, 200 dead MedSpa carried from the pre-pivot niche). Real touches against ALL 398 leads since 07-31: 7 no-answer, 1 gatekeeper, 1 phone-verified, 1 phone-found, 1 lost, 1 callback-set — 12 total in 9 days. Yesterday's routine handed the user a specific 14-lead reactivated dial list (Premier Restoration → Burggraf); zero of those 14 show any touch today. `metrics.md` is still the single 07-27 row. This is not a sourcing problem — sourcing more leads into a pile nobody is calling fails the CEO filter harder each day it repeats | TBD — resume sourcing only once pipeline.md shows the user is actually working through the existing 198 restoration leads, not before | Keep (test) |
| 2026-08-09 | Paused new-lead sourcing a 3rd day; surfaced dial misallocation, not just dial volume | Restoration pipeline (198) already at the routine's own 200-lead hard cap — sourcing more breaches it outright. But the picture changed from 08-08: 143 real dials happened later that day (git log, not reflected in the 08-08 report written at 10am), so this is no longer purely a "nobody is calling" problem. 139 of 143 dials (97%) hit MedSpa — the niche killed 07-31 for zero sales — while 162 of 198 restoration leads have never been dialed once. The constraint is dial *allocation* into a dead niche, not dial volume or lead supply. `metrics.md` still shows the single 07-27 row despite 143 dials since | TBD — resume sourcing once restoration `sourced`-status leads drop under ~50 AND dial hours are verifiably going to restoration, not MedSpa |
| 2026-08-10 | Paused new-lead sourcing a 4th consecutive day — zero movement since | The 08-09 resume condition (sourced-leads under ~50, dial hours verifiably going to restoration) is unmet and, unlike 08-08→08-09, nothing changed at all: git log shows no commits since the 08-09 report — a full day passed with the same 14-lead reactivation list untouched, 162 of 198 restoration leads still never dialed. Restoration carryover (187 `new`/`contacted`) already exceeds the ~150 target and sits 13 short of the routine's own 200 hard cap — sourcing more adds pipeline the business isn't working, fails all 4 filter questions | Restoration pipeline unchanged: 198 total, 162 never dialed, 0 new touches logged | Same resume condition as 08-09, still not met — do not resume until pipeline.md shows real restoration call outcomes accumulating | Keep (test) |
| 2026-08-11 | Paused new-lead sourcing a 5th consecutive day — but flagged the trend reversed | Restoration carryover (183 `new`/`contacted`) still exceeds the ~150 target and sits 17 short of the 200 hard cap, so sourcing more still fails the filter on the same grounds. But unlike 08-08→08-10, real dialing happened: 73 leads moved off `sourced` to a real touch since the 08-10 report, 8 new losses logged, untouched pool dropped 162→88. Also fixed a QA bug: `Best Option Restoration KC` was live in the pipeline despite being flagged a national franchise in this agency's own 08-01 sourcing notes — marked `lost`/excluded | Restoration pipeline: 203 total, 88 never dialed (down from 162), first real movement since the pause started | Resume condition unchanged from 08-09 (sourced-leads under ~50, dial hours going to restoration not MedSpa) — not yet met but on trend to clear in ~2-3 more days like today. `metrics.md` still hasn't been updated since 07-27 despite the activity — that needs a weekly-scoreboard pass regardless of the sourcing decision | Keep (test) |
| 2026-08-12 | Paused new-lead sourcing a 6th consecutive day; substituted enrichment for sourcing | Active restoration carryover (179 = 161 `new` + 18 `contacted`) still exceeds the ~150 target and sits 21 short of the 200 hard cap. Untouched-sourced pool continued its improving trend (88→66) but is still above the 50-lead resume bar set 08-09 — sourcing more still fails the filter on identical grounds. Instead ran `lead-research` in enrichment-only mode on 15 gatekept/no-name leads (gatekeepered first, per the routine's own priority rule) — this reduces the actual gatekeeper bottleneck without growing the oversized backlog | 12 of 15 leads gained a name and/or verified email; 1 stale "possible franchise acquisition" QA flag on ABC Carpet Cleaning traced to an unrelated company and was cleared. Restoration pipeline: 203 total, 66 never dialed (down from 88) | Resume condition unchanged from 08-09 (sourced-leads under ~50, dial hours going to restoration) — on trend to clear in 1-2 more days at this rate. `metrics.md` still not updated since 07-27; this routine can't fill it responsibly without real user-reported call outcomes, so it's not attempting to guess one | Keep (test) |
| 2026-08-13 | Paused new-lead sourcing a 7th consecutive day; also excluded a networked "independent" lead and corrected 2 wrong phone numbers | Active restoration carryover (170 = 151 `new` + 19 `contacted`) still exceeds the ~150 target and sits 30 short of the 200 hard cap. Untouched-sourced pool kept improving (88→66→55) but is still above the 50-lead resume bar — 1 more day at this rate clears it. Ran `lead-research` enrichment on the next 15 gatekept/no-name leads; caught Independent Restoration Services (STL) as a 30+ city irs-247.com templated network wearing an "independent" mask (excluded, moved to `lost`), and 2 phone numbers that didn't match the company (Advantage Kwik-Dry/Flood Pros was dialing the wrong division's line; Butler Plumbing's on-file number didn't match its own site) — both corrected before they wasted a dial | 11 of 15 enriched leads gained a name/email/corrected phone. Restoration pipeline: 203 total, 55 never dialed (down from 66). Untouched pool has been unchanged in composition day-over-day (same 55 companies) — meaning today's dialing, if any, is happening against the gatekeeper-retry/contacted pool, not clearing new ground | **Real constraint, stated plainly: `metrics.md` has shown one row (2026-07-27) through 7 straight reports despite dial activity visible in git log.** This is no longer a lead-supply or even a dial-allocation problem — it's an unmeasured pipeline. Resume-sourcing condition unchanged (untouched <50, ~1 day out). Recommend the next manual session runs the weekly metrics.md fill this cycle, not another sourcing pass | Keep (test) — but escalating the metrics gap, not just repeating it |
| 2026-08-14 | Paused new-lead sourcing an 8th consecutive day — but the real finding today is a stalled pipeline, not the sourcing math | Active restoration carryover (171 = 151 `new` + 19 `contacted` + 1 `booked`) still exceeds the ~150 target, 29 short of the 200 hard cap. Untouched-sourced (55) is byte-identical to yesterday because **zero commits happened anywhere in the repo between the 08-13 10:18 report and this run** — a full selling day with no dials logged, no pipeline edits, no bridge sync. Worse: the pipeline's only `booked` lead, Heartland Flood, Mold & Restoration, had a Zoom demo scheduled for yesterday 1pm and its outcome is still unlogged 24+ hours later — the closest this agency has come to a first dollar of MRR, sitting unresolved. Ran `lead-research` enrichment on the next 15 `Owner TBD` leads instead of sourcing; it also caught a second network-pattern lead (Water Damage Restoration Eldon — near-duplicate domain, different phone, templated multi-state micro-geo pages, same signature as the 08-13 irs-247.com catch) and held it out of the dial list pending human review, plus a BBB-says-defunct-but-site-is-live conflict on Century Restoration & Construction | 5 of 15 enriched leads gained a confirmed name, 3 more gained an unverified directory name, 3 emails added, 1 lead held pending a franchise/network read, 1 lead flagged for an operating-status conflict. Restoration pipeline: 197 total (recount corrected a prior miscount), still 55 untouched | **The constraint is no longer lead supply or even dial allocation — it's that a booked demo's outcome went unlogged for a full day.** Flagged to the user directly via notification: log the Heartland Flood outcome today, and fill `metrics.md`, which has now shown one row since 07-27 across 8 straight reports. Resume-sourcing condition (untouched <50) unchanged and now secondary to this | Keep (test) — escalated past the sourcing question to the unlogged-booked-call gap |
| 2026-08-15 | Paused new-lead sourcing a 9th consecutive day; untouched-sourced pool stopped improving | Active restoration carryover (175 = 156 `new` + 19 `contacted`, Heartland moved back to `contacted` since 08-14 — Marcus is not the buyer) still exceeds the ~150 target, 25 short of the 200 hard cap. Untouched-sourced held flat at 55 for the second straight day (88→66→55→55→55) — the improving trend from 08-11–08-13 has stalled, meaning dial hours are landing on the gatekeeper/contacted pool, not clearing new ground. Sourcing more still fails the filter on identical grounds. Ran `lead-research` enrichment on the next 15 `Owner TBD` leads; caught a second not-independent lead (Brown Restoration — a division of Phoenix Renovation and Restoration since 2015, already in pipeline under its own name; excluded to `lost`), a lead-gen-shell flag (Mold Remediation Kansas City Solutions — no team content anywhere, copy cloned across directory-template sites; held), and a phone mismatch (In-Tech Carpet Cleaning's own site now shows a different primary number than the one on file). Also corrected one stage-hygiene error found in the file: Ozarks Fire and Water Restoration was still `contacted` a lead that was told "not interested" on 08-11 | 8 of 15 enriched leads gained a confirmed name, 1 more an unverified first name, 2 emails added, 1 lead excluded as non-independent, 1 held for shell-site review, 1 phone mismatch caught before a wasted dial. Restoration pipeline: 203 total, 55 untouched (flat) | Resume condition unchanged (untouched-sourced <50) — but flagging that it's stalled, not just unmet, since dial effort isn't reaching the untouched pool. `metrics.md` has now missed 9 straight weekly cycles (still 1 row since 07-27) — that gap is now the standing constraint, not sourcing math. `objections.md` is still empty | Keep (test) — same resume condition, escalating that it has stopped moving |
| 2026-08-16 | Paused new-lead sourcing a 10th consecutive day; untouched-sourced pool finally cleared the 50-lead resume bar, but active carryover is still the binding number | Active restoration carryover (169 = 150 `new` + 19 `contacted`) is 19 **over** the ~150 target — comfortably inside the 100–200 hard bounds, meaning there is no supply gap to fill, independent of any other threshold. Untouched-sourced dropped 55→49, crossing under the 08-09 resume bar for the first time in 5 days flat — real signal that dial effort finally reached new ground, but it's secondary: the primary carryover-vs-target number has been over target every day of this pause and still is, so sourcing still fails the filter on the same grounds it has for 10 days. Ran `lead-research` enrichment on the next 15 unverified/no-name leads (gatekeepered first); caught one live name conflict before it could cause a bad cold-call (Americlean of Iowa — 3 different Wehmeyers surfaced across sources, left blank) and one phone correction before a wasted dial (Hambly & Sons, corrected to 319-529-9533) | 5 of 15 upgraded to confirmed names, 1 phone corrected, 1 name conflict caught and left blank, 4 candidate names found but held UNVERIFIED (directory-only), 4 emails added. Zero disqualified. Restoration pipeline: 197 total, 49 untouched (first time under 50) | **Resume condition needs updating** — untouched-sourced <50 is met today but active carryover (169) is still over the ~150 target, so don't resume on the untouched metric alone. New resume signal: active carryover trending down toward ~150 (i.e., leads closing out via booked/lost faster than they pile up), not just the untouched subset. `metrics.md` is now 20 days / ~2 missed weekly cycles stale on a single 07-27 row — flagged directly to the user this run, concrete date attached (fill by 08-17) | Keep (test) — escalated the resume condition itself, since the old one no longer captures the real state |
